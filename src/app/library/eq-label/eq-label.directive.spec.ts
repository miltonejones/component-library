import { EqLabelDirective } from './eq-label.directive';

describe('EqLabelDirective', () => {
  it('should create an instance', () => {
    const directive = new EqLabelDirective();
    expect(directive).toBeTruthy();
  });
});
