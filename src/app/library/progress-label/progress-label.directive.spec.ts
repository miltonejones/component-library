import { ProgressLabelDirective } from './progress-label.directive';

describe('ProgressLabelDirective', () => {
  it('should create an instance', () => {
    const directive = new ProgressLabelDirective();
    expect(directive).toBeTruthy();
  });
});
