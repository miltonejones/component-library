import { TrackInfoPipe } from './track-info.pipe';

describe('TrackInfoPipe', () => {
  it('create an instance', () => {
    const pipe = new TrackInfoPipe();
    expect(pipe).toBeTruthy();
  });
});
