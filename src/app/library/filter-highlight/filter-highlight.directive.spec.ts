import { FilterHighlightDirective } from './filter-highlight.directive';

describe('FilterHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new FilterHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
