import { HeroBackgroundDirective } from './hero-background.directive';

describe('HeroBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new HeroBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
